# Backend origin only (no trailing slash). Vite exposes this because of the VITE_ prefix.
VITE_API_URL=http://localhost:3001
# Set to true only if the API is down and you need the in-memory mock.
VITE_USE_MOCK=false
